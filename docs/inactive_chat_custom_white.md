<details class="kui-reasoning-block"><summary>思考过程</summary>

用户想要一个交互式的三角函数展示工具，可以调节参数的那种。这非常适合用 custom 类型的 UI 组件来实现。我可以创建一个带有可调节参数的三角函数可视化工具，包含：

1. 正弦、余弦、正切等三角函数曲线
2. 可调节的振幅、频率、相位等参数
3. 使用 Canvas 或 SVG 绘制
4. 滑块来调节参数

让我设计一个完整的交互式三角函数工具：

- 显示 sin/cos/tan 曲线
- 滑块调节：振幅(A)、频率(ω)、相位(φ)、垂直位移(D)
- 公式显示：y = A·sin(ωx + φ) + D
- 单位圆展示
- 实时更新的曲线图

我会用 HTML Canvas + CSS 来实现，包含：
- 函数选择器（sin, cos, tan）
- 参数滑块
- 实时曲线图
- 单位圆（可选展示）
- 关键值标注

让我写一个完整的实现。

</details>

我来创建一个交互式三角函数可视化工具，学生可以通过滑块调节参数，直观理解振幅、频率、相位和垂直位移对函数图像的影响。